# Phải khớp device trong server/.env khi bật bảo mật HMAC.
# Để trống VITE_HMAC_DEVICE_SECRET nếu muốn dev local không cần khóa.
VITE_HMAC_DEVICE_ID=web-client
VITE_HMAC_DEVICE_SECRET=change-me-web-secret
